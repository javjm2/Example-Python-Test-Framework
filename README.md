# cushion-task

Clone repo

create venv

install requirements

install docker

build the docker image

run the tests in the docker image

pre-commit install

docker build -t selenium-sweet-shop-test .

docker run --rm selenium-sweet-shop-test

issue with Microsoft python

pip install --upgrade pip setuptools virtualenv pre-commit

pre-commit clean

pre-commit install

pre-commit run --all-files
